{
  "name": "threads",
  "version": "1.0.0",
  "description": "",
  "main": "scraper.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../threads-client/ && npm run build && cp -r dist ../threads-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon scraper.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "puppeteer": "^20.8.0"
  }
}
